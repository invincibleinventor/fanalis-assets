# Fanalis — compressed static 3D assets

Draco-compressed GLB models + recompressed textures (~25MB, was ~95MB). Served via Cloudflare Pages (free, unlimited egress, CORS via _headers) to keep heavy transfer off the app host. Paths mirror the app /public.
